<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Fancy Inventory</title>
    <style>
        body {
  font-family: 'Arial', sans-serif;
  background-color: #eaeaea;
  color: #333;
  line-height: 1.6;
  margin: 0;
  padding: 0;
}

h1 {
  color: #222;
  font-size: 30px;
  font-weight: normal;
  margin-top: 20px;
  text-shadow: 1px 1px 0px #fff;
}

button {
  background-color: #5cb85c;
  color: #fff;
  border: 1px solid #4cae4c;
  padding: 10px 20px;
  border-radius: 4px;
  font-size: 18px;
  cursor: pointer;
  outline: none;
  box-shadow: 0 2px 2px 0 rgba(0,0,0,0.05);
}

button:hover {
  background-color: #4cae4c;
}

#products {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-around;
  background-color: #fff;
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0 1px 4px rgba(0, 0, 0, 0.2);
}

.product {
  border: 1px solid #ddd;
  padding: 15px;
  margin: 15px;
  border-radius: 5px;
  background-color: #fafafa;
  width: calc(33% - 30px);
  box-sizing: border-box;
}

.product p {
  margin: 5px 0;
  font-size: 18px;
}

.product strong {
  color: #333;
  font-weight: bold;
}

hr {
  border: 0;
  height: 1px;
  background: #ddd;
  margin: 20px 0;
}
    </style>
</head>
<body>
    <h1>Our Fancy Inventory</h1>
    <button onclick="location.href='/load'">LOAD</button>
    <div id="products">
        <% if (products) { %>
            <% products.forEach(product => { %>
                <div class="product">
                    <p><strong><%= product.title %></strong></p>
                    <p>Price: $<%= product.price %></p>
                    <p>Discount: <%= product.discountPercentage.toFixed(2) %> - Final Price: $<%= (product.price - (product.price * (product.discountPercentage / 100))).toFixed(2) %></p>
                </div>
                <hr>
            <% }) %>
        <% } %>
    </div>
</body>
</html>
